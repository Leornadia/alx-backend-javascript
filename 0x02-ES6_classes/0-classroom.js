export default class ClassRoom {
    // Constructor to initialize the maxStudentsSize property
    constructor(maxStudentsSize) {
        // Assigning the parameter to a private property
        this._maxStudentsSize = maxStudentsSize;
    }
}
